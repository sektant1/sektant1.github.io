// Placeholder for routes whose real page has not landed yet. Every entry
// here is replaced as its page is built.
export function Stub({ name }: { name: string }) {
  return <h1 className="font-mono text-sm">{name}</h1>
}
